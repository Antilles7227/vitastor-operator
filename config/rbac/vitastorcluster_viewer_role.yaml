# This rule is not used by the project vitastor-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to control.vitastor.io resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: vitastor-operator
    app.kubernetes.io/managed-by: kustomize
  name: vitastorcluster-viewer-role
rules:
- apiGroups:
  - control.vitastor.io
  resources:
  - vitastorclusters
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - control.vitastor.io
  resources:
  - vitastorclusters/status
  verbs:
  - get
