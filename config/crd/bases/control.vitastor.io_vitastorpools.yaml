---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: vitastorpools.control.vitastor.io
spec:
  group: control.vitastor.io
  names:
    kind: VitastorPool
    listKind: VitastorPoolList
    plural: vitastorpools
    singular: vitastorpool
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: VitastorPool is the Schema for the vitastorpools API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of VitastorPool
            properties:
              blockSize:
                format: int32
                type: integer
              failureDomain:
                type: string
              id:
                type: string
              immediateCommit:
                type: string
              maxOSDCombinations:
                format: int32
                type: integer
              osdTags:
                type: string
              parityChunks:
                format: int32
                type: integer
              pgCount:
                format: int32
                type: integer
              pgMinSize:
                format: int32
                type: integer
              pgSize:
                format: int32
                type: integer
              scheme:
                type: string
            required:
            - failureDomain
            - id
            - pgCount
            - pgMinSize
            - pgSize
            - scheme
            type: object
          status:
            description: status defines the observed state of VitastorPool
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
